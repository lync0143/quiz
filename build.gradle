plugins {
  id 'application'
}

application {
  // Define the main class for the application.
  mainClass = 'example.quiz.QuizRunner'
}

run {
  standardInput = System.in  
}

wrapper {
  gradleVersion = '7.5.1'
}
